namespace SchoolManagementSystem.Models.Enums
{
    public enum UserRole { Admin, Teacher, Student, Parent }
    public enum UserStatus { Active, Inactive }
    public enum StudentStatus { Active, Inactive, Graduated, Transferred }
    public enum EmployeeStatus { Active, Inactive, Transferred }
    public enum MediumType { English, Sinhala, Tamil }
    public enum EnrollmentStatus { Active, Inactive, Promoted, Completed, Transferred, Graduated }
    public enum AttendanceStatus { Present, Absent }
    public enum ExamType { FirstTerm, MidTerm, EndTerm, Monthly, Weekly, National }
    public enum GradeType { A, B, C, S, W, Null }
    public enum Gender { Male, Female, Other }
    public enum Title { Mr, Ms, Rev }

}


